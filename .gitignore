# .gitignore
# Ignore all files
*

# But do not ignore the .gitignore file itself
!.gitignore

# Also do not ignore the README.md (so people know what the project is about, if needed)
!README.md

# If you have a .gitkeep (optional for empty directories)
!.gitkeep
